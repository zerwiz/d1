# Archive

This folder holds documentation for **non-primary** or deprecated approaches. The Cursor AI is instructed to use the active docs in `/docs` (e.g. RAG_PLANNING.md, RAG_RETRIEVE_OPTIONS.md) for architecture and implementation. Archive docs are for reference only.

| File | Description |
|------|-------------|
| [RAG_RUST_TANTIVY.md](./RAG_RUST_TANTIVY.md) | Optional Rust + Tantivy indexer path. Use when you explicitly want a single-binary, full-text index; otherwise follow the Python JSON approach in RAG_PLANNING.md. |
